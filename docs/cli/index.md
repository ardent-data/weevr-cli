# CLI Reference

Complete reference for all weevr commands and flags.

**Global flags** available on every command:

- `--json` — output in JSON format for machine consumption
- `--version` / `-v` — show version and exit

::: mkdocs-click
    :module: weevr_cli._cli_click
    :command: click_app
    :prog_name: weevr
    :depth: 2
